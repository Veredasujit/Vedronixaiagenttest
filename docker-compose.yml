version: "3.9"

services:
  ai-agent:
    build: .
    container_name: pipecat-agent
    ports:
      - "8007:7860"
    env_file:
      - .env
    restart: unless-stopped
    command: python smartflo_agent.py --transport twilio